[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/github-railway/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/api/socket"
healthcheckTimeout = 120
numReplicas = 1
